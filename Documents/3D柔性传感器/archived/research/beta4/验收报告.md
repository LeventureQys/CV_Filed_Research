# beta4 验收报告

> 日期：2026-07-14  
> 验收范围：beta4 织物垫实物数据研究  
> 验收环境：Windows + PowerShell + Python 3.14 + NumPy/SciPy/Matplotlib

## 1. 编译/运行测试

### B4.1 脚本无语法错误

测试命令：`python import` 各模块

结果：**通过**。Data loader、filters、run_beta4 三个模块导入均无 SyntaxError/ImportError。

### B4.2 数据加载适配

测试命令：分别使用旧格式 CSV 路径和新格式 session 目录调用 `load_recording()`

| 输入 | 格式 | shape | sample_freq_hz | 结果 |
|------|------|-------|----------------|------|
| 背景噪声（旧 CSV） | old | (10, 96) | 10.0 Hz | 通过 |
| 500 g 新 session 目录 | new | (10, 96) | 58.8 Hz | 通过 |

## 2. 功能验收

### B4.3 全算法运行

测试命令：
```powershell
cd Document\Update\v1.0.10 - research
$env:MPLBACKEND='Agg'
python beta4/scripts/run_beta4.py
```

结果：**通过**。7/7 录制成功，0 失败。

### B4.4 指标 CSV 生成

文件：`data/fabric_metrics.csv`

检查结果：**通过**。
- 文件存在
- 行数 = 7 recordings × 12 methods = 84 rows
- 列包含：device, recording, sample_rate_hz, channels, frames, method, mean_total_adc, std_total_adc, peak_to_peak_adc, first_difference_std, roughness_retention
- 所有数值可读

### B4.5 全算法比对图

文件：`output/fabric_all_algorithms.png`

检查结果：**通过**。
- 文件存在，大小约 200 KB
- 显示 7 张子图，每张图 12 组算法叠加

### B4.6 精炼比对图

文件：`output/fabric_final_comparison.png`

检查结果：**通过**。
- 文件存在
- 仅显示 4 组算法：原始、EMA 0.8 s、二阶 IIR 0.5 Hz、鲁棒 EMA

### B4.7 研究报告

文件：`织物垫实物研究报告.md`

检查结果：**通过**。
- 章节完整：研究目的、数据集、算法列表、指标表、关键发现、算法分类评估、放弃算法及其理由、结论、展望

## 3. 数据正确性验证

### B4.8 旧格式采样率

500 g 旧录制 `sample_freq_hz` = 10.0 Hz，与 beta3 一致。**通过**。

### B4.9 新格式采样率

新格式各录制采样率 55.6-62.5 Hz，与 elapsed 列估算吻合（约 17-18 ms 间隔）。**通过**。

## 4. 交付物检查

| 交付物 | 路径 | 状态 |
|--------|------|------|
| 研究报告 | `beta4/织物垫实物研究报告.md` | 通过 |
| 数据加载适配 | `beta4/scripts/data_loader_v2.py` | 通过 |
| 新增算法 | `beta4/scripts/filters.py` | 通过 |
| 主分析脚本 | `beta4/scripts/run_beta4.py` | 通过 |
| 指标 CSV | `beta4/data/fabric_metrics.csv` | 通过 |
| 全算法比对图 | `beta4/output/fabric_all_algorithms.png` | 通过 |
| 精炼比对图 | `beta4/output/fabric_final_comparison.png` | 通过 |
| 指标汇总图 | `beta4/output/fabric_metrics_summary.png` | 通过 |

## 5. 验收结论

**全部验收项通过。** beta4 织物垫实物研究已完成：全量 12 组算法指标提取、精炼至 4 组推荐方案、完整研究报告。

| 层次 | 结果 |
|------|------|
| 编译测试 | 通过 |
| 运行测试 | 通过 |
| 功能完整性测试 | 通过 |
| 交付物完整性 | 通过 |
